import { reduce as cond } from 'conditional-reduce'
class DeclarationComment {
	  static parseSource(source: string)
		{

		}

		constructor(comment: string)
		{

		}
}

export default DeclarationComment;

export { DeclarationComment };
